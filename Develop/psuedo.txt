Var arrays for upper, lower, numbers, and special characters

Prompt for password length
    Check length is between 8 and 128
        If not then alert that it's not the right length
            Restart

Confirm uppercase
Confirm lowercase
Confirm special
Confirm number

Check boolean values
    When true is selected add those characters to the array

Empty array of password-characters

Empty final array
    While i is less than user entered length

for (var i = 0; i <= user entered length; i++) {
    Fill the array;
    Random whole number; between 0-length of password-characters;

    finalPassword is filled with random characters from the true selection of characters;
}

Final password join

Return final password to HTML